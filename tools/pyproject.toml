[project]
name = "gen-sandbox-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
   "colorama",
   "rich",
   "sexpdata",
   "mistune",
]

[dependency-groups]
dev = [
   "mypy",
   "pytest",
   "pytest-cov",
   "ruff",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=shared --cov-report=term-missing"

[tool.ruff]
target-version = "py312"
line-length = 99

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true